render_core_test_file_deps += custom_target(input   : 'increase_number.slang',
                                            output  : '@BASENAME@.cshader',
                                            command : slang_compile_cs,
)

subdir('basic')
subdir('depth_target_sample')
subdir('multiple_passes')
subdir('ray_tracing')
